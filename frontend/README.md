# AI-Based Pest Outbreak Prediction System — Frontend

This is a frontend-only UI scaffold built with Vite + React + Tailwind CSS.

Quick start:

1. Open a terminal and change to the `frontend` folder:

```bash
cd frontend
```

2. Install dependencies:

```bash
npm install
```

3. Start the dev server:

```bash
npm run dev
```

Credentials for UI-only login:

- Email: `admin@gmail.com`
- Password: `Password@123`

Notes:
- This is UI-only. No backend, DB, Docker, or ML modules are created.
- Use Node.js 16+ (recommended 18+).
